import React from 'react';
import { proxy, useQuery } from 'umi';

proxy;
useQuery;

export default function Page() {
  return (
    <div>
      <h1 className="text-red-500">Page index</h1>
    </div>
  );
}
